apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-cm
  labels:
    app: myapp
data:
  environment.yaml: |-
    environment:
      name: production
      mongo:
        host: mongodb
        port: 27017